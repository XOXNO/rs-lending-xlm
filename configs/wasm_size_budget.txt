# WASM size budget, in bytes, per deploy artifact under
# artifacts/wasm/deploy/ (optimized + spec-doc stripped via make deploy-artifacts).
# Format: <wasm filename> <max bytes>

controller.wasm 140000
pool.wasm 42000
common.wasm 21000
flash_loan_receiver.wasm 27000
defindex_strategy.wasm 45000
# Embedding the OpenZeppelin timelock trait adds 8 entrypoints
# (schedule/execute/cancel/update_delay + 4 query defaults), the Operation /
# OperationState types, role symbols, and storage helpers: 46,126 -> 52,628 B.
# TL-5b adds the read-only resolve_market_oracle_config / resolve_oracle_tolerance
# views (CLI timelock-execute support), which pull the MarketOracleConfig and
# OraclePriceFluctuation return types plus MarketOracleConfigInput into the
# governance spec: 52,628 -> 57,429 B.
governance.wasm 58000
