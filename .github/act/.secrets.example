# Copy to .secrets and fill in for act runs that need hosted prover access:
#   cp .github/act/.secrets.example .github/act/.secrets
#
# act loads this via:  --secret-file .github/act/.secrets
CERTORAKEY=