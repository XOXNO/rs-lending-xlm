# Originally auto-generated by codebase-memory-mcp; order corrected by hand.
# Prevent merge conflicts on compressed artifact.
#
# `binary` is a macro for `-diff -merge -text`, so it MUST come before
# `merge=ours` — otherwise it unsets the merge driver and the rule silently
# does nothing. Verify with:
#   git check-attr merge binary -- .codebase-memory/graph.db.zst
# Requires the driver to exist: git config merge.ours.driver true
graph.db.zst binary merge=ours
