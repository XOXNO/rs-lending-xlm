# tests/integration — Coverage Audit (2026-06-17)

_Machine-generated by the `integration-coverage-audit` workflow (8 agents). The ABI-mismatch and zero-coverage findings below were independently re-verified against current `main` before recording._

I have all the data needed to synthesize the report. The task is pure synthesis from the provided JSON, critique, and inventory summaries. Let me produce the comprehensive markdown report.

# Integration-Test Coverage Review — Soroban Lending Protocol (Controller + Governance)

**Prepared for:** Protocol owner — target is FULL endpoint coverage in the release-time e2e suite
**Scope:** Controller (`tests/integration/flows/*.sh`, `scenarios/*.sh`) + Governance (timelock lifecycle)
**Lens:** Live-testnet end-to-end coverage. "Covered" in-env (unit/harness) but never driven on testnet is treated as **uncovered e2e** for governance, per the owner's release-gate goal.

---

## 1. Executive Summary

| Surface | Total endpoints | Covered | Partial | Uncovered (incl. view-uncovered) | Testing-only (skip) |
|---|---|---|---|---|---|
| **Controller** | 68 | 47 | 3 | 17 | 1 |
| **Governance** | 50 (production ABI) | 0 e2e | 0 | 50 | 16 forwarders (skip) |

**Headline:**

- **Controller is broadly covered on the user/strategy/liquidation hot path** but shallow on post-mutation state assertion and missing a cluster of admin/limits/oracle/upgrade endpoints entirely.
- **Governance has ZERO live-testnet coverage.** Every production admin mutation in e2e today goes through `cfg(testing)` **immediate forwarders** that are excluded from the production WASM. The real production path — `propose_* → await real delay → execute` through the timelock — is **never exercised on testnet**. This is the single largest gap and the production trust model (governance-owned controller) is untested end-to-end.

**The four must-fix clusters before code freeze:**

1. **Governance timelock e2e** — at least one representative `propose → await → execute → cancel` lifecycle on live testnet (currently 0).
2. **Governance-owned controller deployment** (`deploy_controller`) — the root of the production trust model, never driven e2e.
3. **Controller endpoints with literal zero coverage**: `upgrade_pool`, `revoke_role`, `set_min_borrow_collateral_usd`, `disable_token_oracle`.
4. **The owner's explicit asks** that are untested or shallow: `update_account_threshold` (P0, runs under `|| true`), `claim_revenue` role-guard, market/e-mode/limits/oracle config read-backs, and the entire governance timelock surface.

---

## 2. Coverage Matrix by Area

Legend: ✅ covered · 🟡 partial · ❌ uncovered · 👁️ view-uncovered (never read) · ⏭️ testing-only skip

### 2.1 User-facing flows (`flows/lifecycle.sh`, `flows/liquidation.sh`, `flows/stress.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `supply` | ✅ | isolated-mode market never created; supply-cap headroom never hit (caps=0); `max_supply_positions` overflow never reverts; e-mode supply only single happy create; duplicate-asset aggregation unasserted |
| `supply` guards | ✅ | zero #14 / paused #1000 / deprecated-emode #301 covered; **supply-cap-exceeded** and **position-limit** reverts missing |
| `borrow` | ✅ | isolated single-debt-asset rule never exercised; borrow-cap (caps=0) untested; `min_borrow_collateral_usd` floor vacuous (never set non-zero) |
| `borrow` guards | 🟡 | **only over-LTV #100**; missing borrow-cap, max-utilization-cap, isolated single-debt, min-borrow-floor, inactive/non-borrowable reverts |
| `withdraw` | ✅ | `to` third-party recipient never used; withdraw-while-paused (callable) unasserted; returned `Vec<(Address,i128)>` paid amounts unasserted |
| `withdraw` guards | ✅ | only LTV-breach #100; pool-cash-limit and dust-floor reverts untested |
| `repay` | ✅ | isolated oracle-free decrement never exercised; zero-amount guard untested; repay-while-paused unasserted |
| `renew_account` | ✅ | only owner-self happy path; non-owner revert (AccountNotInMarket) untested; cannot-revive-archived untested |

### 2.2 Strategies (`flows/strategies.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `multiply` | ✅ | `convert_swap` always None; e-mode multiply (category=0 always) untested; `account_id!=0` adjust untested; only modes 2/3; only XLM↔USDC venue |
| `swap_debt` | ✅ | only partial; full-migration untested; isolated single-debt untested; HF asserted only via aggregate, not isolated to op |
| `swap_collateral` | ✅ | only partial; full swap untested; e-mode collateralizable-constraint untested; isolated untested |
| `repay_debt_with_collateral` | 🟡 | **`close_position=true` full-close branch NEVER tested**; dust-on-full-close untested; isolated/e-mode untested |
| `flash_loan` | ✅ | strong negatives (modes 1–5 xfail); single-asset USDC only; 5bps fee not directly asserted; multi-asset receiver untested; paused-flash-loan #1000 untested |

### 2.3 Liquidation (`flows/liquidation.sh`, `scenarios/liq_20feed.sh`, `liq20_width.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `liquidate` | ✅ | **exact-close boundary untested** (998/1000 fudge avoids the close-factor cap); protocol-fee-on-seize unasserted; multi-collateral seize order unasserted |
| `liquidate` guards | ✅ | only healthy #101; over-close refund amount unasserted |
| `clean_bad_debt` | ✅ | one socialization path only; `require_not_flash_loaning` guard untested; **non-KEEPER caller revert untested**; multi-asset residual untested |

### 2.4 Admin / Config (`flows/admin.sh`, `protocol.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `deploy_pool` | ✅ | one-time; PoolAlreadyDeployed repeat untested |
| `create_liquidity_pool` | ✅ | **AssetAlreadySupported** + **TokenNotApproved** + IRM-reject xfails missing |
| `upgrade_liquidity_pool_params` | ✅ | invalid-IRM reject untested; IRM change not read back |
| `upgrade_pool` | ❌ | **NEVER invoked** — pool-WASM upgrade forwarding, requires-pool-deployed guard, non-owner revert all untested |
| `set_aggregator` / `set_accumulator` / `set_liquidity_pool_template` | ✅ | happy only; non-owner reverts untested (low value) |
| `approve_token` / `revoke_token` | ✅ | revoked-state effect (create now reverts TokenNotApproved) untested; non-owner untested |
| `__constructor` | ✅ | paused-by-default + default-limits 10/10 not directly asserted post-deploy |
| `upgrade` | ✅ | happy + auto-pause side-effect covered; non-owner untested |
| `migrate` | ✅ | non-monotonic revert (InternalError) untested |
| `pause` / `unpause` | ✅ | non-owner reverts untested; `unpause` #1001 already-live covered |

### 2.5 Oracle (bounds / deviation / tolerance / twap / spot)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `set_market_oracle_config` | ✅ | single (Twap) + dual (PrimaryWithAnchor) covered; **sanity-bound reject, Spot-only #38, PairNotActive, quote-market TOCTOU all untested**. **ABI MISMATCH:** harness uses verb `configure_market_oracle` (caller+ORACLE role); current main is `set_market_oracle_config` (owner-only). Harness targets a divergent build. |
| `set_oracle_tolerance` | ✅ | happy + #2000 role-guard; **ABI MISMATCH:** harness passes `--caller/--first/--last`; current main is `set_oracle_tolerance(asset, OraclePriceFluctuation{first_upper/lower,last_upper/lower})` 4 bands, owner-only. 4 bands never tested distinctly; `within_first/second_tolerance` effect unasserted |
| `disable_token_oracle` | ❌ | **NEVER invoked** — ORACLE circuit-breaker (Active→Disabled), PairNotActive reject, non-ORACLE revert, Disabled-effect-on-borrow all untested |

### 2.6 E-mode (`flows/admin.sh`, `flows/liquidation.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `add_e_mode_category` | ✅ | validate_risk_bounds reject untested; returned id unasserted |
| `edit_e_mode_category` | ✅ | EModeCategoryDeprecated reject untested; bounds reject untested; edit not read back |
| `remove_e_mode_category` | ✅ | downstream #301 covered; already-deprecated re-remove untested; cascade-strip unasserted |
| `add_asset_to_e_mode_category` | ✅ | EModeCategoryDeprecated + AssetAlreadyInEmode rejects untested |
| `edit_asset_in_e_mode_category` | ✅ | AssetNotInEmode + deprecated rejects untested; flag change not read back |
| `remove_asset_from_e_mode` | ✅ | AssetNotInEmode reject untested; config strip unasserted |

### 2.7 Limits (`flows/admin.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `set_position_limits` | ✅ | boundary + #36 over-max covered; zero-limit reject untested; enforcement (max+1 positions) untested |
| `set_min_borrow_collateral_usd` | ❌ | **NEVER invoked** — floor effect on borrow, negative-floor InvalidBorrowParams, non-owner all untested. **(P0)** |
| `get_min_borrow_collateral_usd` (view) | 👁️ | **NEVER read** — read-back for the setter untested |

### 2.8 Risk-params (`flows/admin.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `edit_asset_config` | ✅ | **validate_risk_bounds reject untested**; edit read via `get_market_config` but **NOT asserted** ("edit not verified by parse"); e_mode_categories-preservation invariant unasserted |
| `upgrade_liquidity_pool_params` | ✅ (dup §2.4) | invalid-IRM reject + read-back missing |

### 2.9 Revenue / Claim (`flows/admin.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `claim_revenue` | ✅ | happy well-asserted (pool revenue strictly drops); **non-REVENUE role-guard #2000 missing**; NoAccumulator #211, multi-asset claim, paused all untested. **(Owner ask)** |
| `add_rewards` | ✅ | exercised but not directly asserted; non-REVENUE guard + supply-index-raise effect untested |

### 2.10 Keeper (`flows/admin.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `update_indexes` | ✅ | happy over [XLM,USDC,SAC_LIQA]; non-KEEPER, AssetNotSupported, paused reverts untested |
| `update_account_threshold` | 🟡 | **SHALLOW — runs under `\|\| true`, failure silently tolerated**; `has_risks=true` branch never tested; risk-param propagation onto positions never verified; non-KEEPER untested. **(P0, owner ask)** |
| `clean_bad_debt` | ✅ (see §2.3) | non-KEEPER revert untested |

### 2.11 Ownership / Roles (`flows/admin.sh`)

| Endpoint | Status | Missing edge cases |
|---|---|---|
| `grant_role` | ✅ | operational roles covered; non-owner + invalid-symbol untested |
| `revoke_role` | ❌ | **NEVER directly invoked** — InvalidRole reject, non-owner untested. **(P1)** |
| `transfer_ownership` | ✅ | 2-step both directions; `live_until_ledger==0` CANCEL untested; non-owner untested |
| `accept_ownership` | ✅ | 2-step handshake covered; non-pending-owner reject + role re-grant side-effects unasserted |
| `has_role` | ⏭️ | testing-only, excluded from release WASM |

### 2.12 Governance timelock lifecycle (entire surface — 0 e2e)

| Endpoint cluster | Status | Note |
|---|---|---|
| `__constructor`, `deploy_controller`, `controller` | ❌ | **deploy_controller P0** — governance-owned controller deployment never driven on testnet; e2e deploys controller directly via EOA, bypassing governance |
| `execute`, `cancel`, `get_operation_state/ledger`, `hash_operation` | ❌ | **execute/cancel P0** — no live propose→await-real-delay→execute; expired #40, pre-delay #4001/#4002, open-execution (executor=None) all untested live |
| `resolve_market_oracle_config`, `resolve_oracle_tolerance` | 👁️ | byte-parity proven in-env, **never on live Reflector** (TL-6 flagged "NOT live-verified") |
| `propose_set_position_limits` | ❌ | **P0** — the simplest-args op; the representative live lifecycle anchor |
| `propose_configure_market_oracle` | ❌ | **P0** — exercises live oracle probe + resolved-arg byte-parity + real delay + controller TOCTOU in one flow |
| `propose_edit_asset_config`, `propose_create_liquidity_pool`, `propose_upgrade_pool/controller`, `propose_transfer_ctrl_ownership`, `propose_grant_controller_role`, `propose_edit_oracle_tolerance` | ❌ | **P1** — heaviest ScVal payloads + code-upgrade + role allowlist (#41 guard proposer-only) |
| `propose_governance_upgrade` + `execute_governance_upgrade` | ❌ | **P1** — ONLY self-targeted inline-dispatch path (Soroban self-reentry workaround), zero coverage anywhere |
| `propose/execute_update_delay`, `propose/execute_grant/revoke_governance_role`, `propose/execute_transfer_gov_own`, `accept_ownership` | ❌ | **P1** — governance-self ops: mutual-exclusion (#41), monotonic-delay (#39), role-migration handoff all untested |
| `pause` / `unpause` (governance owner-gated) | ❌ | **P1** — production emergency brake, distinct from EOA-direct controller pause already covered |
| Remaining `propose_*` (e-mode, tokens, set_aggregator/accumulator/template, min_borrow, migrate, revoke_controller_role) | ❌ | **P2** — cover via one representative governance-routed-setter scenario |
| 16 `cfg(testing)` immediate forwarders | ⏭️ | Skip — excluded from production WASM; production coverage belongs to the matching `propose_*` |

### 2.13 Views (Controller)

| View | Status | Note |
|---|---|---|
| `can_be_liquidated`, `health_factor`, `total_borrow_in_usd`, `borrow_amount_for_token`, `app_version` | ✅ | well asserted |
| `total_collateral_in_usd`, `collateral_amount_for_token`, `get_account_positions`, `get_account_attributes`, `ltv_collateral_in_usd`, `liquidation_collateral_available`, `get_market_config`, `get_e_mode_category`, `get_all_markets_detailed`, `liquidation_estimations_detailed` | ✅ but smoke | **fetched but NEVER asserted** — `get_market_config` is the verification mechanism for `edit_asset_config`/oracle config but is a dead smoke read |
| `get_all_market_indexes_detailed` | ✅ but smoke | **P1** — exposes `within_first/second_tolerance` + final/safe/aggregator price; the observable surface for oracle deviation, unasserted |
| `account_exists`, `get_pool_address`, `get_min_borrow_collateral_usd`, `max_withdraw`, `max_supply`, `max_borrow`, `get_market_index` | 👁️ | **NEVER read** — `max_*` are key vault-integrator surfaces (P1) |

---

## 3. P0 Gaps — Must-Fix Before Code Freeze

These are endpoints with NO coverage that are user-facing/admin-config, plus the owner's explicit asks. Each is the highest-risk-per-edit item.

### 3.1 Governance timelock e2e (owner ask, P0) — the largest gap

The entire production governance path is untested on testnet. Add ONE coherent live lifecycle, then layer two more representative payloads.

**P0-G1 — `deploy_controller` (governance-owned controller).**
- **Add:** New flow `flows/governance.sh::flow_gov_deploy`. Deploy `governance.wasm` (`stellar contract deploy governance --admin --min_delay=12`), then `gov.deploy_controller(controller_wasm_hash)`.
- **Assert:** returned controller addr `== gov.controller()` view; the controller's owner `== governance contract` (not an EOA); `DeployControllerEvent` emitted; before deploy, `gov.controller()` panics `PoolNotInitialized`; a `min_delay=0` deploy reverts `#39`; `has_role(admin, {ORACLE,PROPOSER,EXECUTOR,CANCELLER})==true`; `get_min_delay()==12`.

**P0-G2 — `propose_set_position_limits` → await → `execute` → `cancel` (representative lifecycle).**
- **Add:** `flow_gov_timelock_lifecycle`. `gov.propose_set_position_limits(proposer, …, salt)` → poll `get_operation_state` through the real min_delay ledgers → `gov.execute(Some(executor), …)`.
- **Assert:** full `Unset→Waiting→Ready→Done` chain via `get_operation_state`; `get_operation_ledger`==0 pre-propose, ==(propose_ledger+min_delay) while Waiting, ==1 after Done; controller `PositionLimits` actually updated; `hash_operation(...)` == the BytesN returned by `propose_*`. Then a **separate** propose+`cancel` asserting state→`Unset` and a follow-on execute reverts `#4002`. Add `non_executor` #2000 and `non_canceller` #2000 negatives, and an `executor=None` open-execution variant.

**P0-G3 — `propose_configure_market_oracle` (live oracle probe + resolved-arg byte-parity).**
- **Add:** `flow_gov_oracle_config`. `gov.propose_configure_market_oracle(real market)` → `resolve_market_oracle_config(asset,cfg)` view via `--send=no` → poll to Ready → `execute` with the view output **verbatim** as args.
- **Assert:** if execute succeeds the byte-parity holds live; `get_market_config.oracle_config == resolved`; this exercises live Reflector probing + the controller-side `require_quote_markets_active_usd` TOCTOU re-check (SEC LOW-1) in one flow. This is the TL-6 confirmation flagged "NOT live-verified" in memory.

### 3.2 Controller endpoints with literal zero coverage

**P0-C1 — `set_min_borrow_collateral_usd` + `get_min_borrow_collateral_usd` (owner ask: update limits).**
- **Flow:** `flows/admin.sh`. Set the floor to a non-zero WAD; read back via `get_min_borrow_collateral_usd` and assert equality; then xfail a small borrow below the floor; xfail a negative `floor_wad` (`InvalidBorrowParams`).

**P0-C2 — `update_account_threshold` (owner ask: update positions risk) — currently fake-passing.**
- **Fix:** Remove `|| true` at `flows/admin.sh:42`. Run **both** `has_risks=false` AND `has_risks=true` on a real seeded liquidation account (place after a liquidation account exists). Assert the post-call liquidation threshold / HF actually changed. Add a non-KEEPER xfail (#2000). Today this is effectively untested.

**P0-C3 — `disable_token_oracle` (owner ask: oracle ops).**
- **Flow:** `flows/admin.sh` (ORACLE role). `disable_token_oracle` on an Active mock market → assert `get_market_config.status==Disabled` → xfail a borrow on it. Add non-ORACLE caller xfail (#2000) and disable-on-non-Active xfail (`PairNotActive`).

### 3.3 Owner explicit asks — config read-backs (currently dead smoke reads)

**P0-C4 — `edit_asset_config` verified by parse (owner ask: update market configs).**
- **Fix:** PARSE `get_market_config` after `edit_asset_config_admin` and assert `LTV=6500/threshold=7000/bonus=900/status` landed AND `e_mode_categories` were preserved. Add a `validate_risk_bounds` xfail (e.g. `ltv>threshold`). The view is fetched but never asserted today.

**P0-C5 — `claim_revenue` role-guard (owner ask: claim revenue).**
- **Note:** core path is well-asserted (pool revenue strictly drops). Add a **non-REVENUE-role** `claim_revenue` xfail (#2000) to prove the role gate, plus a multi-asset claim.

### 3.4 Resolve the oracle ABI mismatch (blocks all oracle e2e correctness)

The harness invokes `configure_market_oracle` (caller+ORACLE) and `edit_oracle_tolerance` (caller+two scalars), but **current main** exposes `set_market_oracle_config` (owner-only) and `set_oracle_tolerance(asset, OraclePriceFluctuation{4 bands})`. The harness is targeting an older/divergent controller build. **Reconcile which WASM the testnet deploy uses before writing any oracle assertions** — otherwise the oracle tests validate a contract that won't ship.

---

## 4. P1 / P2 Gaps — Missing Edge Cases on Covered Endpoints

### 4.1 P1 (high-value)

**Isolated-mode (entirely absent across the suite):** no isolated market is ever created. This leaves untested: `supply` is_isolated flip, `borrow` IsolatedBasis single-debt-asset rule, `repay` oracle-free isolated decrement, and isolated paths in `swap_debt`/`swap_collateral`/`repay_debt_with_collateral`. **Add an isolated-asset market in `flow_liq_setup`** and thread it through supply→borrow→repay with the single-debt-asset xfail. One market unlocks ~5 endpoint edge cases.

**Cap reverts (caps=0 everywhere):** supply-cap, borrow-cap, and max-utilization-cap reverts are structurally untestable today. **Add a capped market** (`supply_cap>0`, `borrow_cap>0`) and xfail at breach.

**`repay_debt_with_collateral close_position=true` (P1):** the only explicitly-flagged untested strategy branch. Add on the multiply account once debt is small; assert the debt position is removed and no dust-floor revert.

**`upgrade_pool` (P1):** add to `flow_admin_upgrade` — re-upload `pool.wasm`, forward the hash, assert the pool still answers a view (e.g. `protocol_revenue`).

**`revoke_role` (P1):** grant→revoke round-trip on a throwaway account + InvalidRole xfail (revoke a role never granted).

**Oracle deviation observability (P1):** drive a dual-oracle divergence (move primary vs anchor apart) and assert `get_all_market_indexes_detailed.within_first_tolerance/within_second_tolerance` flip. This is the only observable surface for the deviation logic and is unasserted.

**Vault-integrator views `max_withdraw`/`max_supply`/`max_borrow` (P1, never read):** for each, read the boundary then assert exact-amount succeeds and amount+1 reverts #100; assert `0` while paused; assert `i128::MAX` on uncapped `max_supply`.

**`create_liquidity_pool` reverts (P1):** `AssetAlreadySupported` (re-create) + `TokenNotApproved` (skip approve) xfails.

**`upgrade_liquidity_pool_params` (P1):** invalid-IRM xfail (`optimal>=max`) + read-back via pool view.

**`add_e_mode_category` / `edit_e_mode_category` (P1):** `validate_risk_bounds` xfail; deprecate-then-edit → `EModeCategoryDeprecated` xfail + read-back.

### 4.2 P2 (incremental)

- **Strategies:** full-migration `swap_debt` (assert old `borrow_amount_for_token==0`); full `swap_collateral` (assert old collateral==0); e-mode multiply (`account_id!=0` adjust + `convert_swap=Some`).
- **Liquidation:** exact-close on a price-frozen mock market (the 998/1000 fudge means the close-factor boundary is never hit); assert `protocol_fees` + `bonus_rate_bps` from `liquidation_estimations_detailed` vs on-chain; assert refund on over-close.
- **`clean_bad_debt` / `update_indexes`:** non-KEEPER #2000 xfails; `update_indexes` AssetNotSupported xfail.
- **`flash_loan`:** paused #1000 xfail; assert exact 5bps fee.
- **`renew_account`:** non-owner xfail.
- **`migrate`:** non-monotonic xfail.
- **`revoke_token`:** post-revoke `create_liquidity_pool` → `TokenNotApproved` xfail.
- **E-mode asset ops:** `AssetAlreadyInEmode` / `AssetNotInEmode` xfails.
- **Smoke-view assertions (cheap, high-signal):** `account_exists` false after full-close; `get_pool_address`==deploy return; `get_account_positions` map keys; `get_account_attributes.owner==ALICE`; `ltv_collateral_in_usd < total_collateral_in_usd`; `coll_usd_alice > 0`.

---

## 5. Efficiency & Structure Recommendations (Ranked)

From the harness critique. The resume/idempotency core and fee-less `sim_probe` frontier discipline are excellent; the waste is concentrated in three places.

| # | Recommendation | Value / Effort |
|---|---|---|
| 1 | **Gate `sim-error` in `assert_green.sh`** and resolve the `update_account_threshold` `\|\| true`-vs-`FAIL`-row contradiction (`flows/admin.sh:42`). Today a probe that errors for a non-budget reason (malformed arg, wrong account) records `sim-error` and **passes the CI gate silently** — the only class where a real defect sails through. | Highest / ~10 lines |
| 2 | **Make `fetch_resources` opt-in (`RESOURCES=1`).** Every successful `inv` (`lib/invoke.sh:49`) fires a second `getTransaction` (30s timeout) + XDR-decode subprocess purely for report columns. ~150 serial RPC round-trips removed; sim path already has the numbers free. Default off for `full_e2e`, on for stress/liq20. | Biggest wall-clock win / one `if` |
| 3 | **Fix two confirmed bugs:** remove top-level `local liq20_debt_pre` (`scenarios/liq_20feed.sh:98` — errors `local: can only be used in a function`, limps on under `set -uo pipefail`); delete dead `agg_route_hex_out`/`agg_quote_out`/`AGG_AMOUNT_IN` (`lib/aggregator.sh:30-50`). | Trivial / correctness |
| 4 | **Add phase-level done-markers** to mutation flows (`LIFECYCLE_DONE`, `STRATEGIES_DONE`, `LIQ_SINGLE_DONE`, `ADMIN_DONE`). Today `flow_lifecycle` etc. have no top-level marker, so resume re-runs destructive sequences (full-close withdraw removes the account) and fails confusingly. | Medium / low |
| 5 | **Collapse the five `liq20_*` width scripts** into one parameterized `liq20_v2_walk` (sim-first, dynamic leeway — strictly the best). Removes a large redundant serial research block + magic-value drift. | High maintainability / medium |
| 6 | **Eliminate redundant re-pricing sweeps.** Stress/liq20 re-prices the same 20 markets ~4× ($1→$1). Skip setup-time `set_mock_price …$WAD` loops (markets don't need a price until first use); collapse the 20-feed reprice + crash to the minimal feed set read. | Medium / medium |
| 7 | **Extract shared helpers + constants block.** One `feed_args <start> <count> <amount>` builder (replaces ~8 copy-paste loops in `stress.sh`/`liq_20feed.sh`/`liq20_width.sh`); one `errors.sh` mapping every `#NNN` (`#100`,`#101`,`#14`,`#36`,`#211`,`#301`,`#1000`,`#1001`,`#2000`…) to a named pattern so negative tests are refactor-safe. | Medium / compounding |
| 8 | **Add post-mutation assertions** where cheapest: collateral/debt deltas after the three strategy swaps (`flows/strategies.sh:66-102` have no post-assertion beyond aggregate `hf_post_strategies`); parse-and-compare `get_market_config` after admin edits (`flows/admin.sh:25-29`); sim-probe the exact liquidation close instead of the 998/1000 fudge. Converts "didn't revert" into "produced correct state." | Medium |

**Also flagged:** `set_mock_ts` (`lib/oracle.sh:48`) is defined but never called — staleness coverage (#206/#38 stale-feed) is a latent claim the suite doesn't honor; wire a staleness xfail or remove. The `superseded` status (`lib/report.sh:34`) is a dead legend entry, never emitted.

---

## 6. Concrete Action Plan — Ordered, PR-Sized Groups

Each group is one coherent change. Ordering puts the gate-correctness and bug fixes first (cheap, unblock trustworthy CI), then the owner asks, then the governance e2e (largest), then breadth.

### PR 1 — Gate correctness + confirmed bugs (do first; everything else relies on a trustworthy gate)
- `scenarios/assert_green.sh`: treat `sim-error` as unresolved unless explicitly whitelisted.
- `flows/admin.sh:42`: remove `|| true` on `update_account_threshold`; record a tolerated status or let the `FAIL` row gate.
- `scenarios/liq_20feed.sh:98`: drop `local`.
- `lib/aggregator.sh:30-50`: delete dead `agg_route_hex_out`/`agg_quote_out`/`AGG_AMOUNT_IN`.
- `lib/oracle.sh:48`: remove `set_mock_ts` or wire its staleness xfail (decide); `lib/report.sh:34`: drop `superseded`.

### PR 2 — Owner asks: controller admin read-backs + zero-coverage limits/oracle (`flows/admin.sh`)
- `edit_asset_config`: PARSE `get_market_config`, assert LTV/threshold/bonus/status + e_mode_categories preserved; add `validate_risk_bounds` xfail.
- `set_min_borrow_collateral_usd` + `get_min_borrow_collateral_usd`: set→read-back→below-floor xfail→negative-floor xfail.
- `update_account_threshold`: `has_risks=false` AND `=true` on a seeded account, assert threshold/HF change, non-KEEPER xfail.
- `disable_token_oracle`: disable→assert Disabled→borrow xfail; non-ORACLE #2000; non-Active `PairNotActive`.
- `claim_revenue`: non-REVENUE #2000 xfail + multi-asset claim.
- `upgrade_liquidity_pool_params`: invalid-IRM xfail + read-back.
- `add_e_mode_category`/`edit_e_mode_category`: bounds xfail + deprecate-then-edit `EModeCategoryDeprecated` xfail + read-back via `get_e_mode_category`.
- **Prerequisite:** reconcile the oracle ABI mismatch (`configure_market_oracle`/`edit_oracle_tolerance` vs `set_market_oracle_config`/`set_oracle_tolerance` 4-band) — confirm the testnet WASM ABI before writing the oracle asserts.

### PR 3 — Zero-coverage controller endpoints: upgrades + roles (`flows/admin.sh`, new `flow_admin_upgrade`)
- `upgrade_pool`: re-upload `pool.wasm`, forward hash, assert pool view still answers.
- `revoke_role`: grant→revoke round-trip + InvalidRole xfail.
- `migrate` non-monotonic xfail; `revoke_token`→`create_liquidity_pool` `TokenNotApproved` xfail; `create_liquidity_pool` `AssetAlreadySupported` xfail.

### PR 4 — Isolated + capped markets (unlocks ~8 user/strategy edge cases) (`flows/liquidation.sh::flow_liq_setup`, `flows/lifecycle.sh`)
- Add one isolated-asset market + one capped market in setup.
- Isolated: supply flips `is_isolated` (assert `get_account_attributes`); borrow second-debt-asset xfail (single-debt rule); oracle-free isolated repay decrement.
- Capped: supply-cap-breach xfail; borrow-cap-breach xfail; `max_supply_positions` overflow xfail.

### PR 5 — Strategy + liquidation depth + vault views (`flows/strategies.sh`, `flows/liquidation.sh`, `flows/lifecycle.sh`)
- `repay_debt_with_collateral close_position=true` (assert position removed, no dust revert).
- Full-migration `swap_debt` (old==0); full `swap_collateral` (old==0); e-mode multiply (`account_id!=0`, `convert_swap=Some`).
- Post-swap collateral/debt delta assertions on all three strategy swaps.
- Exact-close liquidation on a price-frozen mock market; assert `protocol_fees`/`bonus_rate_bps`; assert over-close refund.
- `max_withdraw`/`max_supply`/`max_borrow`: boundary-exact-succeeds / +1-reverts / 0-while-paused / `i128::MAX`-uncapped.
- Cheap smoke-view asserts: `account_exists`, `get_pool_address`, positions/attributes maps, `ltv_collateral_in_usd`, `total_collateral_in_usd`.

### PR 6 — Governance timelock e2e foundation (largest; new `flows/governance.sh`)
- `flow_gov_deploy`: deploy governance + `deploy_controller`; assert controller owner==governance, `gov.controller()` parity, `DeployControllerEvent`, `min_delay=0`→#39, 4-role grant, `get_min_delay`.
- `flow_gov_timelock_lifecycle`: `propose_set_position_limits`→poll `get_operation_state` through real delay→`execute(Some)`→assert controller updated + full state/ledger chain + `hash_operation` parity; separate propose+`cancel`→#4002; non-executor/non-canceller #2000; `executor=None` variant; expired-op #40 variant.

### PR 7 — Governance representative payloads + self-ops (`flows/governance.sh`)
- `flow_gov_oracle_config`: `propose_configure_market_oracle`→`resolve_market_oracle_config` view→execute with verbatim output→assert `get_market_config.oracle_config==resolved` (live Reflector probe + controller TOCTOU). Same for `propose_edit_oracle_tolerance` + `resolve_oracle_tolerance`.
- `flow_gov_heavy_payloads`: `propose_edit_asset_config` (17-field ScVal round-trip), `propose_create_liquidity_pool` (2-struct payload), `propose_upgrade_pool`/`propose_upgrade_controller` (assert paused side-effect), `propose_transfer_ctrl_ownership` (+non-contract negative), `propose_grant_controller_role` (+#41 unknown-role — proposer-only guard).
- `flow_gov_self_ops`: `propose/execute_governance_upgrade` (inline self-dispatch), `propose/execute_update_delay` (#39 zero + monotonic negatives), `propose/execute_grant_governance_role` (#41 EXECUTOR/CANCELLER mutual-exclusion), `propose/execute_transfer_gov_own`→`accept_ownership` (assert 4-role migration old→new), `gov.pause`/`gov.unpause` (emergency brake → supply #1000 → #1001).
- One representative `flow_gov_routed_setter` covering the remaining P2 `propose_*` (e-mode set, tokens approve/revoke round-trip, set_aggregator/accumulator/template, min_borrow i128-encoder, migrate, revoke_controller_role).

### PR 8 — Efficiency cleanup (after coverage lands, to avoid churn-on-churn)
- `RESOURCES=1` gate around `fetch_resources` (`lib/invoke.sh:49`); default off for `full_e2e`.
- Phase-level done-markers (`LIFECYCLE_DONE`/`STRATEGIES_DONE`/`LIQ_*_DONE`/`ADMIN_DONE`).
- Collapse five `liq20_*` width scripts into one parameterized `liq20_v2_walk`.
- Remove redundant re-pricing sweeps; collapse 20-feed reprice/crash to the minimal read set.
- Extract `feed_args` helper + `errors.sh` named error-code constants (also de-risks every negative test in PRs 2–7 — consider landing `errors.sh` first if PR 2 is large).

**Coverage outcome:** PRs 1–5 bring the **controller** from 47/3/17 to full coverage (every endpoint invoked, every owner ask asserted, every named revert exercised). PRs 6–7 bring **governance** from 0 e2e to full live-testnet timelock lifecycle coverage including the self-targeted inline-dispatch path. PR 8 removes the measured wall-clock waste without altering coverage.
